import React from 'react';

const Order = () => {
    return (
        <div style={{textAlign: 'center'}} className="mb-5">
            <button>Checkout Your Order</button>
            <h5 style={{color:'red'}}>Click On Food for information and add to cart.</h5>
        </div>
    );
};

export default Order;